// Test-all-primitives scenario: one of each kind (triangle, pyramid, cylinder,
// ellipse, ellipsoid) in a fitted grid. The triangle alone is a separate
// scenario (00_primitive_triangle.js).
cairns.dispatch("cairns.primitive.createAll", {});
