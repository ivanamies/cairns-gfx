.DS_Store
build/
tmp/
imgui.ini

# media assets (incl. third-party) — kept local, not tracked
assets/*.glb
# ...except the fly rig, which a committed golden (fly_pose) depends on. Every
# other GLB is an external staging asset; this one is generated in-repo by
# tools/mjcf_to_rigged_glb.py and is only ~3 MB.
!assets/fly.glb
assets/*.png
# generated-in-repo effect textures (scripts/make_*.py) ARE tracked
!assets/wc_paper_noise.png
assets/*.jpeg
assets/*.jpg
assets/*.ogg
assets/*.ttf
assets/*.svg
assets/a.out
assets/a.out.dSYM/

*~
*swp
*spv
!assets/*.spv
PERFORMANCE_THROWAWAY.md

# stray compile output (repo root) + android asset staging (build.gradle MOBILE_ASSETS_DIR)
a.out
a.out.dSYM/
app/src/main/assets/
